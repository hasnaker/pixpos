apiVersion: v1
kind: Service
metadata:
  name: mega-pos-api
  namespace: mega-pos
  labels:
    app: mega-pos-api
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: mega-pos-api
